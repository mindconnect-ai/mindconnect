<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ai.mindconnect</groupId>
        <artifactId>mc-agents-parent</artifactId>
        <version>${revision}${sha1}${changelist}</version>
        <relativePath>../../mc-agents-parent/pom.xml</relativePath>
    </parent>

    <artifactId>mc-agent-runtime-builder</artifactId>
    <name>mc-agent-runtime-builder</name>
    <packaging>jar</packaging>
    <description>Spring-free builder facade for embedding the agent runtime in any Java program.
        Compiles against every capability module (tools, vector stores, file store, workflows) but
        declares them optional: a client sees and links only the aspects whose modules it puts on
        its own classpath — the rest of the builder API simply stays unused.</description>

    <dependencies>
        <!-- The one mandatory dependency: the runtime core (brings the LLM gateway). -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-llm-gateway</artifactId>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-message-repository</artifactId>
        </dependency>
        <!-- usePostgres(): every store in the database -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-runtime-pg</artifactId>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-message-repository-pg</artifactId>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-llm-gateway-pg</artifactId>
        </dependency>

        <!-- Everything below is OPTIONAL: present at compile time so the builder
             can offer typed APIs for every aspect, but never forced onto the
             client's classpath. Add the module to your own pom to use its aspect;
             tool factories of absent modules simply report unavailable. -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-tools</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-tools-document</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-tools-web</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-tools-code</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-agent-tools-workflow</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-vector-store</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-vector-store-pgvector</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-vector-store-tools</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-file-store</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-admin-rest</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-file-store-pg</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- usePostgres() keeps workflows in the database too, when the workflow modules are present -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-persistence-pg</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
