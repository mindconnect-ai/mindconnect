<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ai.mindconnect</groupId>
        <artifactId>mc-workflow-parent</artifactId>
        <version>${revision}${sha1}${changelist}</version>
        <relativePath>../mc-workflow-parent/pom.xml</relativePath>
    </parent>

    <artifactId>mc-workflow-admin-app</artifactId>
    <name>mc-workflow-admin-app</name>
    <packaging>jar</packaging>

    <description>
        Standalone Spring Boot app for the workflow admin. Runs the embeddable
        mc-workflow-admin-rest module against a file-based store, in SPA mode
        (no SSR, no auth). Doubles as a smoke target for the admin library.
    </description>

    <dependencies>
        <!-- The embeddable workflow admin (auto-configures the controller + store) -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-admin-rest</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-persistence-pg</artifactId>
        </dependency>
        <!-- Seeds the bundled example workflows on first run. -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-initial-data</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Language modules so Code steps can run in the standalone app -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-javascript</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- Dev only: restart on recompile + LiveReload for static assets. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
