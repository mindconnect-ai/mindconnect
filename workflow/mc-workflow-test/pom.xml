<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
<parent>
        <groupId>ai.mindconnect</groupId>
        <artifactId>mc-workflow-parent</artifactId>
        <version>0.0.4-SNAPSHOT</version>
        <relativePath>../mc-workflow-parent/pom.xml</relativePath>
    </parent>

    <groupId>ai.mindconnect</groupId>
    <artifactId>mc-workflow-test</artifactId>
    <name>mc-workflow-test</name>
    <version>0.0.4-SNAPSHOT</version>
    <packaging>jar</packaging>

    <description>
        Integration tests exercising the full workflow stack:
        core engine + Jackson serialization + HTTP + code steps
        (JavaScript, BeanShell, Jython). No production sources.
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <lombok.version>1.18.40</lombok.version>
    </properties>

    <dependencies>
        <!-- Core engine -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow</artifactId>
            <version>0.0.4-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <!-- Jackson serialization: load workflows from JSON -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-jackson</artifactId>
            <version>0.0.4-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <!-- JavaScript via GraalJS -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-javascript</artifactId>
            <version>0.0.4-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <!-- BeanShell -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-beanshell</artifactId>
            <version>0.0.4-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <!-- Jython -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-jython</artifactId>
            <version>0.0.4-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <!-- Groovy -->
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-groovy</artifactId>
            <version>0.0.4-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>

        <!-- Test framework -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.26.3</version>
            <scope>test</scope>
        </dependency>

        <!-- Lombok for test helpers -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0</version>
                <configuration>
                    <!-- GraalJS + Jython + Groovy + BeanShell need ample heap -->
                    <argLine>-Xmx2g -Xms512m -XX:+UseG1GC</argLine>
                    <systemPropertyVariables>
                        <polyglot.engine.WarnInterpreterOnly>false</polyglot.engine.WarnInterpreterOnly>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
