<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
<parent>
        <groupId>ai.mindconnect</groupId>
        <artifactId>mc-workflow-parent</artifactId>
        <version>0.2.1</version>
        <relativePath>../mc-workflow-parent/pom.xml</relativePath>
    </parent>

    <groupId>ai.mindconnect</groupId>
    <artifactId>mc-workflow-swing-ui</artifactId>
    <name>mc-workflow-swing-ui</name>
    <version>0.2.1</version>
    <packaging>jar</packaging>

    <description>
        Swing desktop UI for creating, editing, saving, loading and executing workflows.
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mainClass>ai.mindconnect.workflow.swing.ui.WorkflowDesignerApp</mainClass>
    </properties>

    <dependencies>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow</artifactId>
            <version>0.2.1</version>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-jackson</artifactId>
            <version>0.2.1</version>
        </dependency>
        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-dsl-puml</artifactId>
            <version>0.2.1</version>
        </dependency>

        <!--
          GraalJS — JSR-223 JavaScript engine (replaces Nashorn removed in JDK 15).
          Registers itself under "javascript", "js", "graal.js" at runtime.
        -->
        <!-- PlantUML — local (in-process) diagram rendering; no internet required -->
        <dependency>
            <groupId>net.sourceforge.plantuml</groupId>
            <artifactId>plantuml</artifactId>
            <version>1.2023.13</version>
        </dependency>

        <dependency>
            <groupId>org.graalvm.js</groupId>
            <artifactId>js-scriptengine</artifactId>
            <version>25.0.2</version>
        </dependency>
        <dependency>
            <groupId>org.graalvm.js</groupId>
            <artifactId>js-language</artifactId>
            <version>25.0.2</version>
        </dependency>

        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-beanshell</artifactId>
            <version>0.2.1</version>
        </dependency>

        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-jython</artifactId>
            <version>0.2.1</version>
        </dependency>

        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-javascript</artifactId>
            <version>0.2.1</version>
        </dependency>

        <dependency>
            <groupId>ai.mindconnect</groupId>
            <artifactId>mc-workflow-code-groovy</artifactId>
            <version>0.2.1</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>
            <!--
              maven-shade-plugin builds the fat jar and correctly MERGES
              META-INF/services/* files from all jars (critical for JSR-223
              engine discovery — without this, only the last engine file wins).
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <!-- App module, never published — no reduced POM needed;
                                 keeps it out of the module dir / git. -->
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>jar-with-dependencies</shadedClassifierName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>${mainClass}</mainClass>
                                    <manifestEntries>
                                        <!-- GraalJS requires reflective access to java.lang internals -->
                                        <Add-Opens>java.base/java.lang=ALL-UNNAMED java.base/java.lang.reflect=ALL-UNNAMED java.base/java.io=ALL-UNNAMED java.base/java.util=ALL-UNNAMED</Add-Opens>
                                        <!--
                                          macOS Retina / HiDPI: these JVM system properties must be visible
                                          to the launcher.  WorkflowDesignerApp.main() also sets them
                                          programmatically (belt-and-suspenders), but embedding them here
                                          makes `java -jar` invocations pick them up automatically on
                                          JVM implementations that honour JavaFX/AWT manifest entries.
                                          Note: standard `java -jar` does NOT read these; the programmatic
                                          approach in main() is the authoritative fix.
                                        -->
                                        <Apple-AWT-Application-Appearance>system</Apple-AWT-Application-Appearance>
                                    </manifestEntries>
                                </transformer>
                                <!-- Merges all META-INF/services/* files so every JSR-223 engine is discoverable -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <!-- Keeps NOTICE/LICENSE files from all jars -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer"/>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
