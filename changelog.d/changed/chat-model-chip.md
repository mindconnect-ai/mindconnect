- **chat:** **the composer's model button names the model, not the config.**
  It said `agent-default`, which tells nobody what the chat runs on. It now
  shows the model behind the agent's LLM config — for an alias, the model of
  the config the alias points at — and falls back to the config's name when
  there is none. The button (and the folder button beside it) keeps the
  card's rounded corners under every theme instead of turning into a pill.
