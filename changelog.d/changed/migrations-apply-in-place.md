- **agents:** **Applying a migration in the Admin UI no longer reloads the page.** Apply, a per-field Apply and Apply all answer with a patch: the button spins while the import runs, then the applied item (or diff row) leaves the list, its section disappears when it was the last of its type, and the section count and "Apply all (n)" follow — open sections and scroll position stay as they were.
