- **agents:** **the admin UI renders with semantic-ui 0.4.2** (was 0.4.0). What
  a screen can now use: `UiDrawer` (a panel from an edge that minimizes to a
  handle; on a narrow screen a bottom sheet) and `UiDrawerGroup`, a menu in a
  form's button bar (`UiAction.menu`, with `UiMenuItem.heading`), a rich-text
  editor whose toolbar stays put, `UiCustom` for a plugin's node type without
  a Java class, and icon sets from the asset registry. Fixed on the way: a
  ticked checkbox is visible again in the clody theme.
