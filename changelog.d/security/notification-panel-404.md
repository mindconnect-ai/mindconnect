- **agents:** **The admin UI's namespace guard and write guard are in force again.** Both were component-scanned configurations with a `@ConditionalOnBean` on beans that only auto-configurations define — scanning runs first, so the condition never matched and neither was installed: a plain member of a namespace reached its admin routes, and agents, LLM configs, skills, workflows and MCP servers were written without the admin check. They now look the namespace service up at the request and at the write.
