# Builds the deployable image and pushes it to GHCR.
#
# The server never builds anything: it pulls
# ghcr.io/mindconnect-ai/mc-agent-admin-ui and starts it. Old versions live in
# the registry rather than on the host's disk, which is what makes a small box
# workable.
name: docker-images

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-images-${{ github.ref }}
  cancel-in-progress: true

jobs:
  images:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build jar
        run: >
          mvn -B --no-transfer-progress
          -pl agents/server/mc-agent-admin-ui-app -am
          install -DskipTests

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ── admin UI ────────────────────────────────────────────────────────────
      - id: meta-admin
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/mc-agent-admin-ui
          tags: |
            type=sha,format=long
            type=ref,event=branch
            type=semver,pattern={{version}}

      - uses: docker/build-push-action@v6
        with:
          # Context is the module's target directory: the build only needs the
          # jar, and shipping the whole repo would send gigabytes to the daemon.
          context: agents/server/mc-agent-admin-ui-app/target
          file: agents/server/mc-agent-admin-ui-app/Dockerfile
          push: true
          tags: ${{ steps.meta-admin.outputs.tags }}
          labels: ${{ steps.meta-admin.outputs.labels }}
          cache-from: type=gha,scope=admin-ui
          cache-to: type=gha,mode=max,scope=admin-ui

      # ── code_execute's python image ─────────────────────────────────────────
      # The default image of the python language (CodeLanguages.PYTHON_IMAGE).
      # Built for arm64 too: most people running the admin UI locally are on a
      # Mac, and an amd64-only image would run emulated or not at all.
      - uses: docker/setup-qemu-action@v3

      - id: meta-code-exec-python
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/code-exec-python
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=long
            type=semver,pattern={{version}}

      - uses: docker/build-push-action@v6
        with:
          context: agents/core/mc-agent-tools-code/images/python
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-code-exec-python.outputs.tags }}
          labels: ${{ steps.meta-code-exec-python.outputs.labels }}
          cache-from: type=gha,scope=code-exec-python
          cache-to: type=gha,mode=max,scope=code-exec-python
